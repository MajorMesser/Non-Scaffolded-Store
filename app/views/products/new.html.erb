<%= form_for @product do |f| %>
	<p>
		<%= f.label :name %>
		<%= f.textfield :name %>
	</p>
	<p>
		<%= f.label :description %>
		<%= f.text_area :description %>
	</p>
	<p>
		<%= f.label :price %>
		<%= f.textfield :price %>
	</p>
	<p>
		<%= f.submit "New Product" %>
	</p>
<% end %>